<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lld360.cnc.repository.UserClickHabitDao">

    <insert id="create" parameterType="com.lld360.cnc.model.UserClickHabit">
        INSERT INTO `user_click_habit`(
          `user_id`,
          `site`,
          `method`,
          `ip`
        )VALUES(
          #{userId},
          #{site},
          #{method},
          #{ip}
          )
    </insert>

    <select id="getDocActive" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM
        (SELECT DISTINCT
        `user_id`
        FROM
        `user_click_habit`
        WHERE `site` LIKE '%doc%' AND DATE(`create_time`) = DATE(#{searchTime})
        ) AS u
    </select>

    <select id="getPostsActive" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM
        (SELECT DISTINCT
        `user_id`
        FROM
        `user_click_habit`
        WHERE `site` LIKE '%post%' AND DATE(`create_time`) = DATE(#{searchTime})
        ) AS u
    </select>

    <select id="getSoftActive" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM
        (SELECT DISTINCT
        `user_id`
        FROM
        `user_click_habit`
        WHERE `site` LIKE '%soft%' AND DATE(`create_time`) = DATE(#{searchTime})
        ) AS u
    </select>

    <select id="getVideoActive" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM
        (SELECT DISTINCT
        `user_id`
        FROM
        `user_click_habit`
        WHERE `site` LIKE '%video%' AND DATE(`create_time`) = DATE(#{searchTime})
        ) AS u
    </select>

    <select id="getCourseActive" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM
        (SELECT DISTINCT
        `user_id`
        FROM
        `user_click_habit`
        WHERE `site` LIKE '%course%' AND DATE(`create_time`) = DATE(#{searchTime})
        ) AS u
    </select>

</mapper>