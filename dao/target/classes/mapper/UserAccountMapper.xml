<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lld360.cnc.repository.UserAccountDao">

    <!-- 定义全局查询的SQL变量语句 -->
    <sql id="searchCondition">
        <where>
             status=1
			<if test="userId != null">and user_id = #{userId}</if>
			<if test="account != null">and account like "%"#{account}"%"</if>
			<if test="accountType != null">and account_type = #{accountType}</if>
			<if test="accountName != null">and account_name = #{accountName}</if>
			<if test="status != null">and status = #{status}</if>
			<if test="bankName != null">and bank_name = #{bankName}</if>
        </where>
    </sql>
	<!-- 用于select查询公用抽取的列 -->
	<sql id="columns">
	    <![CDATA[
        	id ,
        	user_id ,
        	account ,
        	account_type ,
        	account_name ,
        	status,
        	bank_name
	    ]]>
	</sql>

    <select id="search" parameterType="map" resultMap="useraccountMap">
        SELECT
        <include refid="columns"/>
        FROM  user_account
        <include refid="searchCondition"/>
        <if test="limit != null">LIMIT #{limit}</if>
        <if test="offset != null">OFFSET #{offset}</if>
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT count(1)
        FROM user_account
        <include refid="searchCondition"/>
    </select>

    <select id="find" parameterType="long" resultMap="useraccountMap">
        SELECT
        <include refid="columns"/>
        FROM  user_account
        WHERE id = #{id}
    </select>

    <insert id="create" parameterType="com.lld360.cnc.model.UserAccount" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO
        user_account (
        	user_id ,
        	account ,
        	account_type ,
        	account_name ,
        	status,
        	bank_name
        ) VALUES (
        	#{userId} ,
        	#{account} ,
        	#{accountType} ,
        	#{accountName} ,
        	#{status},
        	#{bankName}
        )
    </insert>

    <update id="update" parameterType="com.lld360.cnc.model.UserAccount">
        UPDATE user_account
        <set>
			<if test="userId != null">user_id = #{userId}, </if>
			<if test="account != null">account = #{account}, </if>
			<if test="accountType != null">account_type = #{accountType}, </if>
			<if test="accountName != null">account_name = #{accountName}, </if>
			<if test="status != null">status = #{status}, </if>
			<if test="bankName != null">bank_name = #{bankName}, </if>
        </set>
        WHERE id = #{id};
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM user_account WHERE id = #{id}
    </delete>

    <select id="isExist" resultType="boolean" parameterType="map">
        SELECT count(1) from user_account WHERE account=#{account} and user_id = #{userId}
    </select>


    <!-- 结果集mapper -->
    <resultMap id="useraccountMap" type="com.lld360.cnc.model.UserAccount">
		 <result property="id" column="id"/>
		 <result property="userId" column="user_id"/>
		 <result property="account" column="account"/>
		 <result property="accountType" column="account_type"/>
		 <result property="accountName" column="account_name"/>
		 <result property="status" column="status"/>
		 <result property="bankName" column="bank_name"/>
    </resultMap>

</mapper> 
