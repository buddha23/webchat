<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lld360.cnc.repository.SoftFileDao">


    <insert id="create" parameterType="com.lld360.cnc.model.SoftDoc" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO
            soft_file (
                uuid,
                file_name,
                file_path,
                file_type,
                file_size,
                file_md5,
                create_time
            ) VALUES (
            #{uuId},
            #{fileName},
            #{filePath},
            #{fileType},
            #{fileSize},
            #{fileMd5},
            #{createTime}
        )
    </insert>
    
     <delete id="delete" parameterType="long">
        DELETE FROM soft_file
        WHERE id = #{id}
      </delete>

    <update id="update" parameterType="com.lld360.cnc.model.SoftDoc">
        UPDATE soft_file
        <set>
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="fileType != null">file_type = #{fileType},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="fileMd5 != null">file_md5 = #{fileMd5},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </set>
        WHERE id = #{id};
    </update>

    <sql id="columns">
	    <![CDATA[
        d.id,
        d.uuid,
        d.file_name,
        d.file_path,
        d.file_type,
        d.file_size,
        d.file_md5,
        d.create_time
        ]]>
	</sql>


    <select id="findByUuId" parameterType="long" resultMap="softMap">
    SELECT
        <include refid="columns"/> 
        FROM soft_file d
        WHERE d.uuid = #{uuId}
    </select>
 
    <select id="find" parameterType="long" resultMap="softMap">
    SELECT
        <include refid="columns"/> 
        FROM soft_file d
        WHERE d.id = #{id}
    </select>

<!-- 结果集mapper -->
    <resultMap id="softMap" type="com.lld360.cnc.model.SoftDoc">
        <id property="id" column="id"/>
        <result property="uuId" column="uuid"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="fileType" column="file_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="fileMd5" column="file_md5"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

</mapper>
