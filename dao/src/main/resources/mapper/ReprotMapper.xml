<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lld360.cnc.repository.ReportDao">

    <sql id="searchCondition">
        <where>
            <if test="beginTime != null">AND create_time &gt;= #{beginTime}</if>
            <if test="endTime != null">AND create_time &lt;= #{endTime}</if>
            <if test="platform != null">AND platform = #{platform}</if>
        </where>
    </sql>

    <select id="searchRegistReport" parameterType="map" resultType="com.lld360.cnc.model.ReportData">
        SELECT
        ${units} AS lookTime,
        COUNT(1) AS userNum
        FROM user
        <where>
            <if test="beginTime != null">AND create_time &gt;= #{beginTime}</if>
            <if test="endTime != null">AND create_time &lt;= #{endTime}</if>
            <if test="platform != null">AND platform = #{platform}</if>
        </where>
        GROUP BY lookTime
        ORDER BY lookTime ASC
    </select>

</mapper>