<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lld360.cnc.repository.SoftDownloadDao">

    <sql id="searchCondition">
        <where>
            d.state != 9
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="softId != null">AND soft_id = #{softId}</if>
            <if test="costScore != null">AND cost_score = #{costScore}</if>
            <if test="evaluate != null">AND evaluate = #{evaluate}</if>
            <if test="createTime != null">AND create_time = #{createTime}</if>
        </where>
    </sql>

    <sql id="columns">
        id, user_id userId, soft_id softId, cost_score costScore, evaluate, comment, create_time createTime
	</sql>

    <select id="countBySoft" resultType="java.lang.Long">
        SELECT COUNT(1) FROM soft_download WHERE uuid = #{uuId}
    </select>
    <select id="search" parameterType="map" resultType="com.lld360.cnc.model.SoftDownload">
        SELECT
        dd.id, dd.user_id userId, dd.soft_id softId, dd.cost_score costScore, dd.evaluate, dd.comment, dd.create_time
        createTime,u.name uName,u.nickname uNickname,d.title dTitle
        FROM soft_download dd
        LEFT JOIN `user` u ON u.id=dd.user_id
        LEFT JOIN `soft_upload` d ON d.uuid=dd.uuid
        <include refid="searchCondition"/>
        ORDER BY
        <if test="sortBy != null">#{sortBy} DESC,</if>
        dd.create_time DESC
        <if test="limit != null and offset != null">LIMIT #{limit} OFFSET #{offset}</if>
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM soft_download dd
        LEFT JOIN soft_upload d ON dd.uuid=d.uuid
        <include refid="searchCondition"/>
    </select>

    <select id="find" parameterType="long" resultType="com.lld360.cnc.model.SoftDownload">
        SELECT
        <include refid="columns"/>
        FROM soft_download
        WHERE id = #{id}
    </select>

    <select id="hasDownload" resultType="java.lang.Boolean">
        SELECT COUNT(id) > 0 FROM soft_download WHERE user_id=#{userId} AND uuid=#{uuId}
    </select>

    <select id="countByDoc" resultType="java.lang.Long">
        SELECT COUNT(1) FROM soft_download WHERE uuid = #{uuId}
    </select>

    <insert id="create" parameterType="com.lld360.cnc.model.SoftDownload" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO soft_download (
            user_id,
            uuid,
            cost_score,
            evaluate,
            comment,
            create_time
        ) VALUES (
            #{userId},
            #{uuId},
            #{costScore},
            #{evaluate},
            #{comment},
            NOW()
        )
    </insert>

    <update id="update" parameterType="com.lld360.cnc.model.SoftDownload">
        UPDATE soft_download
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="uuId != null">uuid = #{uuId},</if>
            <if test="costScore != null">cost_score = #{costScore},</if>
            <if test="evaluate != null">evaluate = #{evaluate},</if>
            <if test="comment != null">comment = #{comment},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </set>
        WHERE id = #{id};
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM soft_download WHERE id = #{id}
    </delete>

</mapper> 
